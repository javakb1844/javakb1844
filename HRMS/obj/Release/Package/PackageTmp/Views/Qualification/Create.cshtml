@model Data.HRMS.LookQualification
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<div class="content-wrapper">
    <h3>
        Add Qualification
    </h3>
    <div class="row">
        <div class="col-sm-6 col-lg-offset-3">
            <!-- START panel-->
            <div class="panel panel-default">
                <div class="panel-heading"> Qualification info</div>
                <div class="panel-body">
                    @using (Html.BeginForm("Create", "Qualification", FormMethod.Post, new { role = "form", id = "frmInsert" }))
                    {
                        <div class="form-group">
                            <label>Profession Name<span class='required'>*</span></label>
 
                        @Html.DropDownListFor(m => m.LookQualificationTypeId, ViewBag.QualificationType as SelectList, new { @class = "form-control", placeholder = "Qualification" })
                        </div>
                        <div class="form-group">
                            <label>Qualification Name<span class='required'>*</span></label>
                            @Html.TextBoxFor(m => m.QualificationName, new { @class = "form-control", placeholder = "Qualification Name" })
                            @Html.HiddenFor(m => m.LookQualificationId)
                        </div>
                        <div class="col-sm-12 text-center">
                            <input type="submit" class="btn btn-sm btn-primary" value="Save" />
                        </div>
                    }

                </div>
            </div>
            <!-- END panel-->
        </div>

    </div>
</div>
<script>
    // just for the demos, avoids form submit
   /* jQuery.validator.setDefaults({
        debug: true,
        success: "valid"
    });*/
    $("#frmInsert").validate({
        rules: {
            LookDepartmentId: {
                required: true
            },
            DesignationName: {
        required: true
    }
        },
        submitHandler: function (form) {
            form.submit();
        }
    });
</script>



@model Data.HRMS.LookGrade
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<div class="content-wrapper">
    <h3>
        Add Grade
    </h3>
    <div class="row">
        <div class="col-sm-6 col-lg-offset-3">
            <!-- START panel-->
            <div class="panel panel-default">
                <div class="panel-heading"> Grade info</div>
                <div class="panel-body">
                    @using (Html.BeginForm("Create", "Grade", FormMethod.Post, new { role = "form", id = "frmInsert" }))
                    {
                        @*<div class="form-group">
                            <label>Department Name<span class='required'>*</span></label>
                            @Html.DropDownListFor(m => m.LookDepartmentId, ViewBag.Departments as SelectList, new { @class = "form-control", placeholder = "Department" })
                        </div>*@
                        <div class="form-group">
                            <label>Grade Name<span class='required'>*</span></label>
                            @Html.TextBoxFor(m => m.GradeName, new { @class = "form-control", placeholder = "Grade Name" })
                            @Html.HiddenFor(m => m.GradeID)
                        </div>
                        <div class="form-group">
                            <label>Grade Value<span class='required'>*</span></label>
                            @Html.TextBoxFor(m => m.GradeValue, new { @class = "form-control", placeholder = "Grade Value" })
                            @Html.HiddenFor(m => m.GradeID)
                        </div>
                        <div class="col-sm-12 text-center">
                            <input type="submit" class="btn btn-sm btn-primary" value="Save" />
                        </div>
                    }

                </div>
            </div>
            <!-- END panel-->
        </div>

    </div>
</div>
<script>
    // just for the demos, avoids form submit
   /* jQuery.validator.setDefaults({
        debug: true,
        success: "valid"
    });*/
    $("#frmInsert").validate({
        rules: {
            LookDepartmentId: {
                required: true
            },
            DesignationName: {
        required: true
    }
        },
        submitHandler: function (form) {
            form.submit();
        }
    });
</script>